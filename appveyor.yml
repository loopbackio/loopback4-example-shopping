environment:
  matrix:
    - nodejs_version: '8'
    - nodejs_version: '10'

install:
  - ps: Install-Product node $env:nodejs_version
  - node --version
  - npm --version
  - npm install

test_script:
  - npm run test:ci

build: off
skip_branch_with_pr: true
